version: 2.1

workflows:
  test-deploy:
    jobs:
      - test

jobs:
  test:
    docker:
      - image: gradle:jdk18
    steps:
      - checkout
      - run: java --version
      - run: gradle --version 

jobs:
  demo: 
    docker:
      - image: cimg/python
    steps: 
      - checkout
      - run: python --version